spring:
  application:
    name: ops-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://auth.gigcred.example.com/
server:
  port: 8090
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  metrics:
    tags:
      application: ops-service
